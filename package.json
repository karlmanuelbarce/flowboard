{
  "name": "flowboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm --prefix api test",
    "test:coverage": "npm --prefix api run test:coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/karlmanuelbarce/flowboard.git"
  }
}
